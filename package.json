{
  "name": "template-store",
  "version": "1.0.0",
  "private": true,
  "description": "Loja estilosa de templates de sites — todos com 10% de desconto.",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --public-url ./",
    "clean": "rm -rf .cache dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}